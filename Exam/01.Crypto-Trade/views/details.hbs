<main>
    <section id="details-info">
        <h1>Details</h1>
        <div class="coin-image">
            <img src="{{offer.image}}">
        </div>

        <div class="coin-info">
            <div class="coin-text">
                <h1 id="name">{{offer.name}}</h1>
                <h3 id="payment">Payment method: {{offer.payment}}</h3>
                <p id="price"><span>Price: ${{offer.price}}</span></p>
                <p id="description">{{offer.description}}</p>
            </div>
            <div class="product-btn">
            {{#if isAuthenticated}}
                {{#if isOwner}}
                <!--Only for registered user and author of the publication -->
                    <div class="author">
                        <a href="/edit/{{offer._id}}" class="btn-edit">Edit</a>
                        <a href="/delete/{{offer._id}}" class="btn-delete">Delete</a>
                    </div>
                {{else}}
                    {{#if canBuy}}
                        <a href="/buy/{{offer._id}}" class="btn-buy">Buy</a>
                    {{else}}
                        <p class="buy-message">You already bought these crypto coins.</p>
                    {{/if}}
                {{/if}}
            {{/if}}
                
                
            </div>
        </div>
    </section>
</main>